"""Landing page. Intentionally bare for now - a placeholder to land on
before drilling into the LDV Sales section."""

import streamlit as st


def render() -> None:
    st.title("Energy Demand Model")
