# Copy this file to .streamlit/secrets.toml and fill in real values for local dev.
# In Streamlit Community Cloud, set the same keys via the app's Settings -> Secrets
# instead of committing a real secrets.toml.

[auth]
username = "changeme"
password = "changeme"

[github]
data_repo_pat = "github_pat_xxx"   # only needed locally if input_data/ CSV is absent
